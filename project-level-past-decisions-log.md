# Past decisions

Append only. One row per decision. A row is never edited or deleted, a reversal is a new row.

A row exists when a problem was raised and a decision closed it. Nothing else gets a row.
Problem and decision are one line each. An error code, a trace id or a ticket number goes in Ref, never the error itself.

| Date       | Problem                                                                                                                                                                      | Decision                                                                                                                                                          | Ref                                                 |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- |
| 2026-09-12 | A layout render cannot set cookies, so the identity cookies and the request URL cannot come from `capture` on a page load                                                    | `src/proxy.ts` mints `_fbp`, `external_id`, `_fbc` on every matched request, forwards them in the request cookie header and sets `x-meta-capi-url` for the render | nextjs.org/docs/app/api-reference/functions/cookies |
| 2026-09-12 | The design doc never rewrites `_fbc`, Meta requires the most recent `fbclid` to be stored and sent                                                                           | `_fbc` is replaced when the URL carries a `fbclid` different from the stored one                                                                                  | developers.facebook.com fbp-and-fbc                 |
| 2026-09-12 | The design doc writes `meta_identity` only on a new key, so a corrected value would be sent once and the stale one kept                                                      | `meta_identity` is rewritten whenever the merged identity differs from the stored one, the fresh value wins, no key is ever dropped                               | -                                                   |
| 2026-09-12 | Which Graph API version to pin                                                                                                                                               | `v25.0`, released 2026-02-18, available until 2028-07-29                                                                                                          | Graph API changelog                                 |
| 2026-09-12 | A `Retry-After` of minutes on a 429 would hold a server action or a redirect past the host timeout                                                                           | A `Retry-After` above 10 s ends the attempts with `unreachable`, below it is honoured as-is                                                                       | -                                                   |
| 2026-09-12 | A national phone number needs a calling code and the request country is only a guess of it                                                                                   | Optional `META_CAPI_PHONE_COUNTRY` prefixes national numbers, the request country is the fallback, a number starting with `+` or `00` keeps its own code          | -                                                   |
| 2026-09-12 | The access token in the query string would land in every URL log                                                                                                             | The token travels in the JSON body, never in the URL                                                                                                              | -                                                   |
| 2026-09-12 | A redirect route taking any `to` URL is an open redirector                                                                                                                   | The link route honours `tel:`, `sms:`, `mailto:`, `geo:`, same-origin paths and an allowlist of map and WhatsApp hosts, anything else is a 400                    | -                                                   |
| 2026-09-12 | A browser or `next/link` prefetch of the link route would fire a Contact or FindLocation event without a click                                                               | Requests carrying a prefetch header are redirected without capture, link triggers are plain anchors, never `Link`                                                 | -                                                   |
| 2026-09-12 | The retry count and interval were named but not valued                                                                                                                       | 3 attempts in total, 1 s then 2 s between them, 5000 ms per attempt                                                                                               | -                                                   |
| 2026-09-12 | The per-IP limit of the link route was named but not valued                                                                                                                  | 20 captures per IP per 60 s window, in memory, pruned above 10 000 entries                                                                                        | -                                                   |
| 2026-09-12 | The link route's own URL is not the page the click came from                                                                                                                 | `event_source_url` of a link event is the same-origin referer, the route URL otherwise                                                                            | -                                                   |
| 2026-09-12 | The scaffold runs every test in jsdom, the module tests need Node and the live test needs `.env`                                                                             | Vitest `projects`: a `node` project for `src/lib` loading `.env` through `@next/env`, a `jsdom` project for components                                            | -                                                   |
| 2026-09-12 | Where the dev crash on a conversion without matching identifier applies                                                                                                      | Every `NODE_ENV` other than `production` throws, production warns                                                                                                 | -                                                   |
| 2026-09-12 | With partial prefetching a render that read `headers()` still runs in the prefetchable runtime stage, where `Date.now()` is an error and a PageView would fire on a prefetch | `read_request_context` awaits `connection()` before anything else, a capture waits for a real request                                                             | blocking-prerender-current-time                     |
| 2026-09-12 | One conditional `Field` component gave every Meta key the same input, so the keyboard, the autofill token and the capitalization were wrong on five keys out of six          | One exported component per form key in `user_data_fields.tsx`, no props, each carrying its own `type`, `autocomplete`, capitalization and correction settings     | developer.mozilla.org autocomplete                  |
| 2026-09-14 | Link events were the weakest signal and the heaviest part of the module: open redirect, prefetch guard, per-IP limit                                                         | `Contact` and `FindLocation` removed with their routes, Meta is optimised on form conversions only                                                                | -                                                   |
| 2026-09-14 | Working on the site or submitting test forms sent real events to Meta                                                                                                        | One flag, `META_CAPI_ENABLED=false` in `.env`, read in `kill_switch.ts`, gates the only send path and the proxy, absent means enabled                             | -                                                   |
| 2026-09-14 | 58 tests interleaved with the code, accumulating at every change                                                                                                             | Tests live in `src/lib/meta_capi/tests/`, one per decision that costs when it silently breaks, a version deletes the rest                                         | -                                                   |
| 2026-09-14 | The README named the confirmation route `/merci`                                                                                                                             | A confirmation route is always named `/confirmation`                                                                                                              | -                                                   |
| 2026-09-14 | A server action writing `meta_identity` re-renders the page and its layouts, so the root layout sent a second PageView for the same page                                     | `PageView` captures nothing when the request carries `next-action`; the render after `redirect()` is a new request and keeps its PageView                         | nextjs.org/docs/app/getting-started/mutating-data   |
| 2026-09-15 | Three services read the same form, each under its own key names                                                                                             | The form carries the keys of a dictionary in `src/lib/form_contract/`, the standard `autocomplete` names plus `freetext`, each module converts through its own table with the shared loop  | ghl_mail_design_doc.md section 3                    |
| 2026-09-15 | The `name` of an input was the Meta key, so a second service could not read the same input                                                                  | The `name` is a dictionary key converted by the Meta table in `user_data_keys.ts`, `Field` is typed on the dictionary                                                                         | ghl_mail_design_doc.md section 3                    |
| 2026-09-15 | Awaiting the Meta send held the server action for up to three attempts of 5 s                                                                               | `capture` writes `meta_identity` before returning and hands the send to `after()`, `{ ok: true }` means scheduled, the Graph answer is only logged                                            | nextjs.org/docs/app/api-reference/functions/after   |
| 2026-09-15 | Whether a rejected `after()` task blocks the tasks of the other modules is not documented by Next                                                           | Verified on Next 16.3.5 by `src/lib/tests/after_isolation.test.ts` against the real `AfterContext`, each task is caught on its own and the others run; the test could not be seen red first | after-context.js, E468                              |
| 2026-09-15 | `server-only` throws under Node, so a module importing it could not be unit tested                                                                          | Vitest aliases `server-only` to the package's own `empty.js`, the build keeps the real check                                                                                                  | -                                                   |
| 2026-09-15 | The proxy imports the Meta module and `server-only` in the middleware bundle layer was unverified                                                           | Next maps `server-only` to its empty module in the `middleware` layer, every Meta file imports it, `next build` and the Playwright cookie tests prove it                                     | next/dist/build/webpack-config.js                   |
| 2026-09-15 | Several free text inputs share the `freetext` key and `FormData.get()` reads only the first                                                                 | The shared loop reads `getAll()` for every key, trims, drops blanks and joins the values with a blank line                                                                                    | -                                                   |
| 2026-09-15 | Nodemailer 10 ships its own types                                                                                                                           | `nodemailer` 10.0.10 alone, `@types/nodemailer` conflicts with the bundled declarations and is not installed                                                                                  | nodemailer.com                                      |
| 2026-09-15 | The names of the inbox variable, the custom field variable and the e2e address were left open                                                               | `MAIL_INBOX` for the domain mailbox, `GHL_FREETEXT_FIELD_ID` for the custom field, `E2E_LEAD_EMAIL` for the address the e2e submits                                                            | -                                                   |
| 2026-09-15 | GHL requires an opportunity `name` and a form may carry no name field                                                                                       | The name is the given and family names, else the email, else the phone, else `Lead`                                                                                                           | -                                                   |
| 2026-09-15 | A submission without email would open an SMTP connection only to fail on the envelope                                                                       | `send_confirmation` answers `refused` before any SMTP call, the copy to the inbox still leaves after the response                                                                             | -                                                   |
| 2026-09-15 | `submit_lead` returns `{ status: "failed" }` and only a client can read the return value of a server action                                                 | `LeadForm`, the only client component, wraps the action in `useActionState`, disables the fields while pending and renders the failure node the page passes                                  | react.dev useActionState                            |
| 2026-09-15 | Biome's `noLabelWithoutControl` cannot see a control rendered by a conditional inside the label                                                             | `Field` links label and control with `useId` and `htmlFor`, the label still wraps the control, two forms on one page never collide                                                           | -                                                   |
| 2026-09-15 | Which Nodemailer errors mean retrying is pointless                                                                                                          | `EAUTH`, `ENOAUTH`, `EENVELOPE`, `EMESSAGE`, `ECONFIG`, `EMAXRECIPIENTS` are `refused`, every other code or no code is `unreachable`                                                          | nodemailer.com/errors                               |
| 2026-09-15 | Which GHL answers mean retrying is pointless                                                                                                                | A 4xx other than 429 is `refused` with GHL's message, 429, 5xx, a network failure or the 10 s timeout are `unreachable`, no retry                                                             | -                                                   |
| 2026-09-15 | An unparsable `SMTP_PORT` would reach the socket                                                                                                            | It falls back to 465, `secure` is true on 465 only                                                                                                                                            | infomaniak.com faq 468                              |
| 2026-09-15 | The `v3` route of the contact custom fields answers 401 "not yet supported by the IAM Service", and `/custom-fields/object-key/contact` refuses contacts                    | The `freetext` field id is read once, at setup, through `GET /locations/{id}/customFields?model=contact` with `Version: 2021-07-28`, the modules stay on `v3`                              | IAM 401                                             |
| 2026-09-15 | The design doc found no documentation of GHL's answer to a second opportunity for the same contact; observed on the second e2e submission                                   | GHL answers HTTP 400 "Can not create duplicate opportunity for the contact", the module logs `refused` and moves on, the contact is updated by the upsert, the mail copy carries the new message | HTTP 400 duplicate opportunity                      |
| 2026-09-15 | Three templates, plain, Meta, and Meta with GHL and mail, each needed maintenance and a consultant cannot pick one                                                          | One template, `model-page-conversion`, the three kill switches replace the choice; the two older repositories and their skills are removed                                                    | -                                                   |
| 2026-09-15 | Biome 2.5 reads Next's `after` as a test hook, `noDuplicateTestHooks`                                                                                                      | The isolation test imports it as `after_response`, no rule silenced                                                                                                                          | -                                                   |
| 2026-09-15 | TypeScript 7 is a new compiler                                                                                                                                             | Adopted, `verify` green on 7.0.2 with Next 16.3.5                                                                                                                                            | -                                                   |
| 2026-09-17 | `model-page-conversion` names neither the stack nor the service, and a skill named after it does not say it runs something                                                   | The repository is `scaffold-nextjs-meta`, its skills `run-scaffold-nextjs-meta`, `run-scaffold-nextjs-meta-maintain` and `run-scaffold-nextjs-meta-wizard`        | -                                                   |
